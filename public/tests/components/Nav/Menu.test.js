'use strict';

import React from 'react';
import Menu from '../../../src/js/components/Nav/Menu';
import { expect } from 'chai';
import { render } from 'enzyme';

describe('test', () => {
    it('another', () => {
        expect(true).to.equal(true);
    });
});
